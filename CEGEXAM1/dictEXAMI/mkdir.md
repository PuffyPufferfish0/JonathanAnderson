# `mkdir` (Make Directory)

Creates a new, empty directory.

### Examples

- `mkdir DirA` : Creates DirA.
    
- `mkdir "Dir B"` : Creates a directory with a space in the name. _(Without quotes, it would create two directories: `Dir` and `B`!)_