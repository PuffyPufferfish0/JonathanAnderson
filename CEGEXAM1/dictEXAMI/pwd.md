# `pwd` (Print Working Directory)

Outputs the absolute path of the directory you are currently in.

### Usage

- `pwd`
    
- _Output Example:_ `/home/puffy/DirA`
    
- Similar to `Get-Location` in Windows PowerShell.