# `man` (Manual)

Shows the system manual pages for commands. It is the definitive guide for a command's syntax, options, and flags.

### Usage

- `man grep` : Opens the manual for `grep`.
    
- Navigate using arrow keys, press `q` to quit.
    
- Similar to `Get-Help` in PowerShell or `command --help`.