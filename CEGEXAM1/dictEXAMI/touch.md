# `touch`

Creates a new blank file. If the file already exists, it updates the file's access and modification timestamps without changing its contents.

### Examples

- `touch test.txt` : Creates an empty file named `test.txt`.
    
- `sudo touch madewithsudo.txt` : Creates a file owned by the root user.